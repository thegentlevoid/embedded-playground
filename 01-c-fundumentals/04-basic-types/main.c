#include <stdio.h>
#include <stdbool.h> // Must include to use boolean type.

int main ()
{
    int age = 22;
    float height = 1.72f;
    char grade = 'A';
    char name[] = "Erfan"; // In C we don't have any string type and to declare strings we do it a an array of chars.
    bool isOnline = true;

    //TODO: cover type conversions and type definitions later.
    return 0;
}